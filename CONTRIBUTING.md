# Contributing

- New decoy tasks must be verified unanswerable and carry a rationale field.
- Grading-rule changes need tests with constructed responses for every class.
- Run `ruff check .` and `pytest` before opening a PR.
